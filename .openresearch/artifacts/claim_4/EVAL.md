# Claim 4 evaluation

Verdict: **BLOCKED**

All four low-confidence routes are complete and materially distinct. They
establish that the public release lacks the code, exact samples, preprocessing,
trained weights, and consistent protocol needed to verify or falsify the
reported real-data numbers. No toy or proxy result is promoted to PASS.

Unblocking requires, at minimum:

1. the exact UAVDT video/frame/track manifest and pure-domain extraction;
2. the exact CMU pure-frame preprocessing and trial evaluation code;
3. trained TRACE and NCTRL weights or reproducible training entrypoints
   consistent with the paper;
4. the exact proxy-normalization and gating evaluation code.
