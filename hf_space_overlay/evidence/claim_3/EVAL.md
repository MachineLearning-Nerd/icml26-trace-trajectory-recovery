# Claim 3 evaluation

Status: **BLOCKED**

The exact paper-scale TRACE side passed: a learned encoder was trained from
scratch for 100 epochs on 200,000 sequences and achieved mean trajectory
correlation `0.973566` across 15 evaluations. The independent checker
recomputed every raw correlation, and time permutation exited `1`.

The full comparative claim remains BLOCKED. Four materially different
low-confidence comparator routes are complete, including the mandatory
falsification route. The exact NCTRL-hard and NCTRL-soft clauses cannot be
verified or falsified from public assets, and TRACE-side success alone cannot
verify them.

The historical directly observed-latent demonstration remains a historical
rejected baseline, not the current full-scale verifier.
