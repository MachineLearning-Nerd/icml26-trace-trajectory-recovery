# Claim 3 pre-run evaluation status

Status: **BLOCKED** pending the exact learned run and faithful NCTRL
comparators.

The current node is designed to replace the earlier directly-observed-latent
toy result with a learned-encoder, paper-scale TRACE measurement. Even if all
pre-registered TRACE-side checks pass, Claim 3 remains BLOCKED until the two
NCTRL variants are run comparably. Raw output will be written to
`run_outputs/paper_scale_learned.json`; the independent checker and negative
control write adjacent JSON files.

The historical toy result is preserved but is not the current verifier.
